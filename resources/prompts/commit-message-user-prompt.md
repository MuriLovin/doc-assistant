Generate a commit message based on the static metadata extracted from the git diff below.
The user input is wrapped in an XML tag named INPUT.
Reply with only the final commit message in one line.

<INPUT></INPUT>
